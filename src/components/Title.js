import React from 'react'
import {FaCocktail, FaHiKing, FaShuttleVan, FaBear } from "react-icons"

export default function Title({title}) {
    return (
        <div className="section-title">
            <h4>{title}</h4>
            <div />
        </div>
    )
}
